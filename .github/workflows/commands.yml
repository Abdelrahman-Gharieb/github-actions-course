on:
  workflow_dispatch:

jobs:
  example:
    runs-on: ubuntu-latest
    steps:
<<<<<<< HEAD
      - name: Check if a file exists
        run: |
          if [ -f "config.yml" ]; then
            echo "::debug::config.yml File exists."
          else
            echo "::error::config.yml File does not exist."
          fi
=======
      - name: Set API Key
        run: echo "API_KEY=12345" >> $GITHUB_ENV
      - name: Use API Key
        run: echo "Using API Key $API_KEY"
>>>>>>> ee8ba062838ecf2de5a8271d9eed2b4990672a93
